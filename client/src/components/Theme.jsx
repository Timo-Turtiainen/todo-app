import { createTheme } from '@mui/material'

export const darkTheme = createTheme({
  palette: {
    mode: 'dark',
    activeBorder: '#58ff4f',
    background: {
      paper: '#121212', // Dark background color
    },
  },
})
