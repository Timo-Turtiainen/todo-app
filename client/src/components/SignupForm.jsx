import { useNavigate } from 'react-router-dom'
import { Typography } from '@mui/material'
function SignupForm() {
  const navigate = useNavigate()
  return (
    <div>
      <Typography>Sign up component</Typography>
    </div>
  )
}

export default SignupForm
